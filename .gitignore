/target

# never commit real service configs or env files
config.toml
config.*.toml
*.local.toml
.env
.env.*

# locally installed agent skills — keep out of the public repo
.agents/
.claude/
skills-lock.json
